spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.healthassistant: DEBUG
    org.springframework.ai: DEBUG
    org.springframework.security: DEBUG
